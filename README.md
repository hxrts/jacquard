# Jacquard

Adaptive routing for ad hoc shaped networks. The shared routing contract supports multiple engines with deterministic admission, typed ownership, and explicit observation boundaries. First party support is provided for mesh routing, with an extension boundary for external routing engines. Routing state is fully deterministic, enabling exact simulation replay.

## Development

```
# enter dev shell
nix develop

# build workspace
just build
```
